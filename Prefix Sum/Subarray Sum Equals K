Description: 
Given an array of integers nums and an integer k, return the total number of subarrays whose sum equals to k.
A subarray is a contiguous non-empty sequence of elements within an array.

Example 1:
Input: nums = [1,1,1], k = 2
Output: 2

Sol: 
class Solution:
    def subarraySum(self, nums: List[int], k: int) -> int:
        res = 0
        curSum = 0
        prefixSums = {0:1}
        
        for i in nums:
            curSum += i
            diff = curSum - k

            res+= prefixSums.get(diff,0)
            prefixSums[curSum] = 1+ prefixSums.get(curSum, 0)
        return res

Approach:

     ~ Maintain a running prefix sum while iterating through the array.
     ~ Use a hashmap (freq) to store how many times each prefix sum has occurred.
     ~ For every new prefix sum curSum, compute curSum - k.
     ~ If curSum - k exists in the map, it means a subarray ending at this index has sum = k.
     ~ Add the frequency of curSum - k to the count.
     ~ Update the map with the current prefix sum.

T.C: O(n)
S.C: O(n)
